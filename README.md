# frontend-code-test-jordi-avella

## Install & run for dev

`npm install`

`npm run dev`


## Install & build for production

`npm install`

`npm run build`

Then, start local server with

`npm run start`

Or serve the public folder
